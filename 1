要在 Kali Linux 上监听另一台电脑的 HTTPS 流量，并截取其中的用户名、密码和 Cookie，您可以使用工具如 Wireshark 结合 SSL/TLS 密钥来实现。以下是一般的步骤：

    设置 Wireshark 捕获 HTTPS 流量：
        打开 Wireshark。
        选择正确的网络接口，然后开始捕获流量。
        使用过滤器过滤出 HTTPS 流量。您可以使用过滤器 tcp.port == 443 来捕获所有的 HTTPS 流量。

    设置 SSL/TLS 密钥：
        如果您要解密 HTTPS 流量，您需要 SSL/TLS 密钥。
        如果是浏览器访问的网站，您可以在浏览器中安装 SSL/TLS 密钥，Wireshark 将会自动检测到。
        如果是其他应用程序，您可能需要手动提供 SSL/TLS 密钥。

    捕获登录界面：
        通过 Wireshark 过滤器或手动分析捕获的流量，找到登录界面的请求。
        确定登录请求的特征，如 HTTP POST 请求包含用户名和密码字段。

    提取用户名、密码和 Cookie：
        一旦捕获到登录请求，分析其中的数据包，提取出用户名、密码和 Cookie 等信息。
        这可能需要对 HTTPS 流量进行解密，以便查看明文的请求内容。

    显示输出：
        将提取的用户名、密码和 Cookie 输出显示在终端或保存到文件中
